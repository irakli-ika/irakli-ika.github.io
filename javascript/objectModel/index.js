const user = {
    name: 'alex',
    age: 30,
    userInfo() {
        return `${this.name} -> ${this.age}`
    }
}

// console.log(user.userInfo())

// console.log(user.message())

//console.log(user.toString())

const arr = [1,2,3]

console.log(arr)

console.log(user)

